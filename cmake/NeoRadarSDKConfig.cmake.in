@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Make the PluginPackager.cmake module available
set(NeoRadarSDK_CMAKE_DIR "${CMAKE_CURRENT_LIST_DIR}")
list(APPEND CMAKE_MODULE_PATH "${NeoRadarSDK_CMAKE_DIR}")

# Include the packaging utilities
include(PluginPackager)

include("${CMAKE_CURRENT_LIST_DIR}/NeoRadarSDKTargets.cmake")

check_required_components(NeoRadarSDK)